## 클로저
void 반환값으로 객체을 사용할수있는점
bodycondition 쓸때는 기존이 아닌 () 로 들어가서 접근해야한다.

함수내에서 기존값과 인스턴스초기값 은 같기에
변경전 값과 변경후 값을 따로 변수에 저장하여 값을 사용한다.

절대값 사용으로 값의 변경값을 알 수 있게 되었다.


## 느낀점
운동객체을 스트럭트로 했다면 값이 계속 새값으로 나타나기에 최종값을 나타낼수없었을거같다.
스트럭트와 클래스 차이를 한번더 느낄수있었다.

